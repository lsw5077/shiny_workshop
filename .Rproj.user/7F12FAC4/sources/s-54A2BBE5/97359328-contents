library(shiny)
library(tidyverse)
library(viridis)

# UI for an app that plots small mammal mass as a function of hindfoot length

ui <- fluidPage(
    
    navbarPage("Portal data",
        tabPanel("Graph"),
        tabPanel("Citations")
    )

    
)
    
    server <- function(input, output) {


}

# Run the application 
shinyApp(ui = ui, server = server)
